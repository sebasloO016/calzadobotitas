<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <style>
        body { display: flex; min-height: 100vh; flex-direction: column; margin: 0.5cm; background-color: #ffffff; }
        .container-fluid { flex: 1; display: flex; flex-wrap: wrap; }
        .navbar-vertical { background-color: #c474a4; height: auto; border-radius: 15px; margin-right: 15px; padding: 15px; flex: 1; max-width: 250px; }
        .navbar-vertical .nav-link { background-color: #532c83; color: #ffffff; transition: background-color 0.3s, color 0.3s; margin-bottom: 2mm; border-radius: 8px; padding: 10px 15px; text-align: center; }
        .navbar-vertical .nav-link:hover { background-color: #14737c; color: #ffffff; }
        .navbar-vertical .nav-link.active { background-color: #14737c; color: #ffffff; }
        .main-content { border-radius: 15px; background: #f8f9fa; padding: 15px; flex: 3; display: flex; flex-direction: column; align-items: center; }
        .navbar-vertical .nav-header { color: #ffffff; font-size: 1.5em; margin-bottom: 20px; text-align: center; }
        .user-name { color: #ffffff; font-size: 1em; margin-bottom: 20px; text-align: center; }
        .logoBotitas { width: 100%; max-width: 250px; margin-bottom: 20px; }
        .button-container { display: flex; flex-wrap: wrap; gap: 20px; justify-content: center; align-items: center; width: 100%; }
        .menu-button { display: flex; flex-direction: column; align-items: center; width: 140px; height: 140px; background-color: #fff; border: 2px solid #ddd; border-radius: 10px; box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); text-align: center; text-decoration: none; color: #333; font-family: Arial, sans-serif; transition: background-color 0.3s ease; }
        .menu-button:hover { background-color: #f0f0f0; }
        .menu-button img { width: 60px; height: 60px; margin-top: 10px; }
        .menu-button span { margin-top: 10px; font-size: 14px; }
        .button-row { display: flex; justify-content: center; width: 100%; flex-wrap: wrap; }
        .button-row .menu-button { margin: 10px; }
        @media (max-width: 768px) { .navbar-vertical { margin-bottom: 15px; max-width: 100%; } .main-content { padding: 10px; flex: 1 1 100%; } .navbar-vertical .nav-link { padding: 8px 10px; font-size: 0.9em; } .navbar-vertical .nav-header { font-size: 1.2em; } }
        @media (max-width: 576px) { .navbar-vertical { padding: 10px; } .navbar-vertical .nav-link { font-size: 0.8em; } .navbar-vertical .nav-header { font-size: 1em; } .menu-button { width: 100px; height: 100px; } .menu-button img { width: 50px; height: 50px; } .menu-button span { font-size: 12px; } }
    </style>
</head>
<body>
    <div class="container-fluid">
        <nav class="navbar-vertical p-3">
            <div class="position-sticky">
                <div class="nav-header">Dashboard Empleado</div>
                <div id="user-name" class="user-name"><%= userName %></div>
                <div class="nav-item">
                    <a class="nav-link" href="/auth/logout">Salir</a> <!-- Botón de salir -->
                </div>
            </div>
        </nav>
        <main role="main" class="main-content">
            <img src="imagenes/logo botitas.png" alt="Logo Botitas" class="logoBotitas">
            <div class="button-container">
                <div class="button-row">
                    <a href="/ventas/nueva" class="menu-button">
                        <img src="imagenes/ventas.png" alt="Ventas">
                        <span>Ventas</span>
                    </a>
                    <a href="/stock" class="menu-button">
                        <img src="imagenes/gps.png" alt="Stock y Ubicación">
                        <span>Stock y Ubicación</span>
                    </a>
                    <a href="/reportes/" class="menu-button">
                        <img src="imagenes/informe.png" alt="Reportes">
                        <span>Reportes</span>
                    </a>
                </div>
            </div>
        </main>
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <script>
        // Activar el enlace nav activo basado en la URL actual
        const currentLocation = location.href;
        const menuItem = document.querySelectorAll('.nav-link');
        const menuLength = menuItem.length;
        for (let i = 0; i < menuLength; i++) {
            if (menuItem[i].href === currentLocation) {
                menuItem[i].className += " active";
            }
        }
    </script>
</body>
</html>
