<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Botitas</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary: #6a5acd;
      --secondary: #7b68ee;
      --bg: #f4f6f9;
      --white: #ffffff;
      --danger: #ff4444;
      --text: #333;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', Arial, sans-serif;
    }

    body {
      background: var(--bg);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      flex-direction: column;
      color: var(--text);
      text-align: center;
    }

    .error-card {
      background: var(--white);
      border-radius: 16px;
      padding: 40px 50px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.08);
      max-width: 500px;
      width: 90%;
      animation: fadeIn 0.4s ease;
    }

    h1 {
      color: var(--danger);
      font-size: 2em;
      margin-bottom: 15px;
    }

    p {
      font-size: 1.1em;
      margin-bottom: 25px;
      color: #555;
      line-height: 1.6;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 20px;
      font-size: 15px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.3s ease;
      text-decoration: none;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: var(--white);
      box-shadow: 0 3px 8px rgba(106, 90, 205, 0.3);
    }

    .btn-primary:hover {
      opacity: 0.9;
      transform: translateY(-2px);
    }

    .error-icon {
      font-size: 4em;
      color: var(--danger);
      margin-bottom: 15px;
    }

    footer {
      position: absolute;
      bottom: 20px;
      font-size: 0.85em;
      color: #777;
    }

    @keyframes fadeIn {
      from {opacity: 0; transform: translateY(10px);}
      to {opacity: 1; transform: translateY(0);}
    }

    @media (max-width: 480px) {
      .error-card { padding: 30px 25px; }
      h1 { font-size: 1.6em; }
      .error-icon { font-size: 3em; }
    }
  </style>
</head>
<body>
  <div class="error-card">
    <i class="fas fa-triangle-exclamation error-icon"></i>
    <h1><%= title %></h1>
    <p><%= message %></p>
    <a href="/" class="btn btn-primary">
      <i class="fas fa-arrow-left"></i> Regresar al inicio
    </a>
  </div>

  <footer>© <%= new Date().getFullYear() %> Botitas — Todos los derechos reservados</footer>
</body>
</html>
