<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Ticket Venta</title>
    <style>
        body {
            width: 80mm;
            font-family: monospace;
            font-size: 12px;
        }

        .center {
            text-align: center;
        }

        .line {
            border-top: 1px dashed #000;
            margin: 5px 0;
        }

        table {
            width: 100%;
        }

        td {
            vertical-align: top;
        }

        @media print {
            button {
                display: none;
            }
        }
    </style>
</head>

<body>

    <div class="center">
        <img src="/img/logo.png" style="max-width: 150px; margin-bottom: 5px;"><br>

        <strong>CALZADO BOTITAS</strong><br>
        RUC: 9999999999001<br>
        Factura: <%= venta.NumeroFacturaSri || 'SIN SRI' %><br>
            Fecha: <%= venta.Fecha %>
    </div>

    <div class="line"></div>

    Cliente: <%= venta.Cliente %><br>
        CI/RUC: <%= venta.Identificacion %>

            <div class="line"></div>

            <table>
                <% detalles.forEach(d=> { %>
                    <tr>
                        <td>
                            <%= d.Cantidad %>x
                        </td>
                        <td>
                            <%= d.Producto %>
                        </td>
                        <td align="right">$<%= (d.PrecioUnitario * d.Cantidad).toFixed(2) %>
                        </td>
                    </tr>
                    <% }) %>
            </table>

            <div class="line"></div>

            <strong>TOTAL: $<%= Number(venta.TotalVenta).toFixed(2) %></strong>
            Forma de pago: <%= venta.FormaDePago %>

                <div class="line"></div>

                <div class="center">
                    ¡Gracias por su compra!
                </div>

                <button onclick="window.print()">🖨️ Imprimir</button>

                <script>
                    window.onload = () => window.print();
                </script>

</body>

</html>